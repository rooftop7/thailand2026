async function loadTrip(){const s=document.getElementById("status");try{const r=await fetch("trip.json?"+Date.now(),{cache:"no-store"});if(!r.ok)throw Error("HTTP "+r.status);const t=await r.json();render(t);s.textContent="Đã tải thông tin mới nhất."}catch(e){console.error(e);s.className="error";s.textContent="Không tải được trip.json. Hãy kiểm tra file trên GitHub."}}function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]))}function render(t){document.title=t.title;title.textContent=t.title;subtitle.textContent=t.subtitle;travelers.innerHTML=t.travelers.map(x=>`<span class="chips">${esc(x)}</span>`).join("");flights.innerHTML=`<div class="card"><b>👥 Chuyến bay chung</b><p>${esc(t.flights.group.note)}</p></div><div class="card"><b>👤 Chuyến bay của Nga</b><p>${esc(t.flights.nga.note)}</p></div>`;stays.innerHTML=[t.airbnbs.bangkok,t.airbnbs.huaHin].map(x=>`<div class="card"><h3>📍 ${esc(x.name)}</h3><p>${esc(x.address)}</p><div class="features">${esc(x.features)}</div><a class="button" href="${x.airbnb}" target="_blank">🏠 Airbnb</a> <a class="button" href="${x.maps}" target="_blank">🗺️ Maps</a></div>`).join("");itinerary.innerHTML=t.itinerary.map(d=>`<div class="item"><div class="date">${esc(d.date)} — ${esc(d.title)}</div>${d.items.map(i=>`<div>• ${esc(i)}</div>`).join("")}</div>`).join("");contacts.innerHTML=Object.entries(t.contacts).map(([n,p])=>`<div class="item"><b>${esc(n)}</b>: ${p?`<a href="tel:${esc(p)}">${esc(p)}</a>`:"Chưa thêm số điện thoại"}</div>`).join("");transport.textContent=t.sections.transport;currency.textContent=t.sections.currency;sim.textContent=t.sections.sim;visa.textContent=t.sections.visa;photos.href=t.photoAlbum;packing.innerHTML=t.packing.map(x=>`<div class="item">${esc(x)}</div>`).join("")}loadTrip();